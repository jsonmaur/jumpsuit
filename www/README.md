# Jumpsuit Marketing Site

- run `npm install`
- run `roots watch` to start development server
- run `roots compile -e production` to build production site
- run `zab deploy` to deploy changes to zab
