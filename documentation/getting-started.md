# How to Install Jumpsuit

To install Jumpsuit on an existing project, run:
```bash
$ npm install jumpsuit --save
```

Once you have a project with `jumpsuit` installed, you're ready to build an app!
